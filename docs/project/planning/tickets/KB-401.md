---
id: "ticket-kb-401"
title: "KB-401 — Knowledge DTO and export service"
type: "ticket"
status: "done"
version: "1.3"
updated: "2026-09-04"
project: "custom-handy"
baseline_id: "handy-main-2026-08-24-af48dd68"
ticket_id: "KB-401"
phase: "4"
owner: "Knowledge Agent"
gate: "G4"
catalog_ref: "TICKET_CATALOG.json#KB-401"
---



# KB-401 — Knowledge DTO and export service

[[planning/tickets/INDEX|← Ticket Index]] · [[planning/phases/PHASE-4|Phase 4]]

**Owner:** Knowledge Agent  
**Dependencies:** [[planning/tickets/DATA-105|DATA-105]], [[planning/tickets/NOTE-301|NOTE-301]], [[planning/tickets/PAD-306|PAD-306]]  
**Allowed ownership:** `knowledge manager/DTOs`

## Goal

Define stable export object independent of SQLite.

## Scope

Implement only the goal and acceptance criteria in this ticket using frozen architecture.

## Out of scope

- architecture redesign;
- unrelated cleanup;
- dependency additions by feature agent;
- shared-file edits unless Task Packet explicitly delegates;
- future-roadmap features.

## Acceptance criteria

- [x] canonical raw=normalized_stt — `KnowledgeItem::raw` is the canonical
      attempt's `normalized_stt`; `engine_raw` is not exported at all
- [x] representations included — every representation of the canonical
      attempt travels with the item
- [x] stable IDs/audio metadata — `export_id` is `capture:<id>` / `note:<id>`,
      deterministic across runs; audio carries file name, sha256, bytes and
      integrity state

## Required tests/evidence

- [x] DTO serialization tests — `knowledge_dto_test`, 8 fixtures: snake_case
      JSON, `contract_version`, RFC3339 UTC timestamps, round-trip

## Escalate instead of deciding if

- schema/public DTO contract needs to change beyond accepted ticket;
- a new dependency is required and not already approved;
- a shared/forbidden file must be edited;
- product/security/data semantics become ambiguous;
- upstream pinned code materially contradicts the frozen contract.

Use [[orchestration/ESCALATION_PROTOCOL|Escalation Protocol]].

## Agent completion

Update RUN_STATE from [[orchestration/templates/RUN_STATE|RUN_STATE Template]], then submit a focused PR to the phase branch.
