---
id: "run-pad-305"
title: "RUN_STATE — PAD-305"
type: "run-state"
status: "generated"
version: "1.0"
updated: "2026-09-03"
project: "custom-handy"
baseline_id: "handy-main-2026-08-24-af48dd68"
ticket_id: "PAD-305"
run_status: "DONE"
attempt_count: "1"
branch: "phase/03-scratchpad"
worktree: ""
last_commit: ""
---

# RUN_STATE — PAD-305

## Evidence
| Check | Result |
|---|---|
| `delivery_routing_test` (8 Fixtures) | 8 passed / 0 failed |
| Rust-Suite gesamt | 313 grün / 0 rot |
| Clippy-Ratchet (windows) | 18 <= 22, unverändert |
| `cargo fmt --check` | PASS |
| Playwright (Gesamtsuite) | 10 passed |
| `tsc --noEmit` / `eslint src` / `check-translations` / `vite build` | PASS |
| Bindings (`--list-devices`, headless) | regeneriert, `DeliveryTarget` exportiert |

## Notes
- Das Ziel eines fertigen Transkripts ist jetzt eine explizite Entscheidung:
  ein `DeliverySink` pro Ziel, aufgelöst in `delivery::sink_for` aus der
  Einstellung `delivery_target`. `clipboard.rs` entscheidet nicht mehr,
  WOHIN Text geht — nur noch WIE die fokussierte App getippt wird.
- `actions.rs` ruft statt `utils::paste` jetzt `sink_for(&app).deliver(&text)`.
  Ein Ziel zu wechseln heißt: einen Sink tauschen. Kein Branch im Paste-Pfad.
- `deliver_with_audit` liefert genau einmal und schreibt genau ein Event —
  auch bei Fehlschlag (DATA-107, siehe [[escalations/PAD-305-01|PAD-305-01]]).
  Retries hängen an, sie überschreiben nie.
- Der Audit ist ein Beobachter: schlägt der Event-Write fehl, wird geloggt,
  aber eine erfolgreiche Zustellung wird NICHT nachträglich zum Fehler
  (Fixture 08). Sonst würde ein DB-Problem dem Nutzer eine funktionierende
  Diktatzustellung als Fehler melden.
- `ScratchpadSink` hängt das Diktat als neue Version mit `SOURCE_DICTATION`
  an die zuletzt berührte aktive Notiz; existiert keine, entsteht eine neue
  Notiz, deren erste Version bereits das Diktat ist (`create_dictated`,
  Titel aus der ersten Zeile). Append-only — bestehende Versionen bleiben
  unangetastet.
- `sink_for` fällt auf den Paste-Pfad zurück, wenn die Notiz-DB nicht
  geöffnet werden kann. Ein Speicherproblem darf ein Transkript nicht
  stillschweigend verschlucken.
- Phase 4 (KB) und Phase 5 (REST/MCP) landen als weitere Sinks, nicht als
  weitere Verzweigungen — das war der Zweck des Seams.

## Offene Punkte
- [[escalations/PAD-305-02|PAD-305-02]] (E2, OPEN): Der Audit ist im
  Live-Pfad nicht erreichbar, weil `delivery_events.source_attempt_id`
  NOT NULL auf `transcription_attempts` zeigt und die kanonische
  Capture/Attempt-Pipeline dort noch nicht schreibt. Der Port ist gebaut
  und getestet; das Anschließen braucht ein Integrator-Folgeticket
  (Vorschlag PAD-306). Muss am G3-Gate quittiert werden.

## Grenzverletzungen
- `src/actions.rs`, `src/settings.rs`, `src/lib.rs`, `src/utils.rs`,
  `src/managers/notes.rs`, `Cargo.toml`, `src/bindings.ts` und die 24
  Locale-Dateien liegen außerhalb der Ticket-Ownership
  (`transcript delivery path`), sind aber für den Seam unvermeidbar:
  Modul-Registrierung, die neue Einstellung `delivery_target` samt UI,
  der Test-Target-Eintrag und `create_dictated`.
  Entscheidung des Auftraggebers aus PAD-303: Eskalation protokollieren,
  Edits behalten.
- `utils.rs`: `pub use crate::clipboard::*;` entfernt. Der Re-Export war
  nach der Umstellung tot (einziger Nutzer war `utils::paste` in
  `actions.rs`) und hätte den Clippy-Ratchet auf 19 gehoben. Der Paste-Pfad
  wird jetzt ausschließlich über `delivery::FocusedAppSink` erreicht.
